INITRD_FILES="bin/sh bin/bash etc/fstab bin/modprobe bin/chroot bin/ls bin/cat bin/mount bin/umount usr/sbin/fsck usr/sbin/fsck.ext2"

# a '.' for this will include all modules
INITRD_MODULES="."

INITRD_SYSFILES=preinit.sh

